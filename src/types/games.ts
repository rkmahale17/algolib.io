export type SortMode = "bubble" | "selection" | "quick";

export interface GameModeInfo {
    id: string;
    title: string;
    description: string;
    difficulty: string;
    icon: any;
    color: string;
}
